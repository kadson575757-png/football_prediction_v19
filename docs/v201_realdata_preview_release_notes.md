# v2.0.1 Realdata Preview Release Notes

This preview supports real match analysis without an Odds API key.

Odds improve confidence but are optional. Without xG, the system should not produce a strong model tip. `NO_BET` is an intentional safe outcome when coverage is weak.

No betting, staking, ROI, profit, money management or guaranteed prediction logic is introduced.
