# v2.0 No-Leakage Backtest

The backtest evaluates historical model quality using each match's own cutoff. It excludes future data and reports accuracy, Brier score, calibration bins, and source coverage.

It does not compute ROI, stake, yield, profit, or betting ledgers.
