# v2.1 No-Leakage Winner Backtest

The v2.1 winner backtest measures model quality, source coverage and calibration without betting metrics.

Metrics include top-1 accuracy, 1X2 Brier score, decision counts and source coverage. ROI, stake and profit are intentionally absent.
