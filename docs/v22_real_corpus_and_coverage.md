# v2.2 Real Corpus and Coverage

v2.2 builds a real multi-league corpus layer before model evaluation.

`matches_total=1` is not a valid model-quality statement. Small samples are marked with `INSUFFICIENT_SAMPLE` and `statistical_validity=LOW`.

The corpus separates completed matches from future fixtures, records result availability, joins xG when available and exports source coverage.
